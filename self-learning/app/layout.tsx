import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NobileBitAcademy - Master Programming Skills',
  description: 'Learn to code with expert instructors, hands-on projects, and career-ready certificates. Frontend, Backend, Fullstack, Data Science paths.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
